// Defines site's metatdata, plugins, other general config
// https://www.gatsbyjs.com/docs/reference/config-files/gatsby-config/

// This is used only for getting gatsby-plutgin-ts-config setup. One JS file is required to have the plugin setup.
// The rest can be typescripts

module.exports = {
  plugins: [
    `gatsby-plugin-ts-config`,
  ]
}
