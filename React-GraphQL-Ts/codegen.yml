overwrite: true
schema: ${SCHEMA_ENDPOINT}
documents: "src/**/*.ts"
generates:
  src/types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
